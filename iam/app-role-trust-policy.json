{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Principal": {
        "Service": "ec2.amazonaws.com"
      },
      "Action": "sts:AssumeRole",
      "Comment": "If app runs on EC2. Change Principal to match your deployment (e.g., lambda.amazonaws.com for Lambda, or use IAM user for Vercel)"
    }
  ]
}
